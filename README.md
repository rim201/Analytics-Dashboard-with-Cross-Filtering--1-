
  # Analytics Dashboard with Cross-Filtering

  This is a code bundle for Analytics Dashboard with Cross-Filtering. The original project is available at https://www.figma.com/design/iPrAHOfiSMBqeQbik1kRih/Analytics-Dashboard-with-Cross-Filtering.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  